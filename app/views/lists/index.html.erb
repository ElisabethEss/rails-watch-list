<h1>These are your lists:</h1>

<ul>
  <% @lists.each do |list| %>
    <li><%= list.name %></li>
        <p>
          <%= link_to "Show this list", list %>
        </p>
  <% end %>
</ul>

<%= link_to "Create a new list", new_list_path %>
